import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'alwin_build_project',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
